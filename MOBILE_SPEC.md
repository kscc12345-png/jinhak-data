# 진학분석기 모바일 버전 구현 지침 (For AI Assistant)

이 문서는 추후 다른 AI가 진학분석기 모바일 버전(Android/iOS)을 개발할 때 참조해야 할 핵심 요구사항 및 데이터 처리 지침을 담고 있습니다. 모바일 환경의 특성과 개인정보 보호를 최우선으로 하여 설계되었습니다.

## 1. 데이터 저장 원칙 (Local Storage Only)
- **개인정보 보호**: 모바일 앱에서 사용자가 입력하는 모든 성적 데이터, 과목 정보, 선택한 대학 목록 등은 **오직 기기 내부(Local Storage)에만 저장**되어야 합니다.
- **네트워크 전송 금지**: 어떠한 경우에도 학생의 성적 데이터를 외부 서버나 클라우드(Firebase 등)로 전송해서는 안 됩니다.
- **앱 삭제 시 데이터 파기**: 기기 로컬 저장소(예: AsyncStorage, SharedPreferences, CoreData, SQLite)를 사용하여, 앱을 삭제할 때 관련 데이터가 함께 완전히 삭제되도록 구현하십시오.

## 2. 학생 데이터 관리 (저장/불러오기 기능)
- 사용자가 여러 번 시뮬레이션하거나 여러 명의 프로필을 관리할 수 있도록, 데스크톱 버전의 students.json과 동일한 역할의 로컬 데이터베이스를 구현해야 합니다.
- **저장 필드**: 반, 번호, 이름(또는 프로필명), 계열(자연/인문), 교육과정, 학기별 성적 입력 데이터 전체.
- **불러오기/수정/삭제**: 메인 화면이나 설정 탭에 "프로필 관리(학생 정보)" 기능을 두어, 저장된 데이터를 언제든 리스트 형태로 확인하고 불러오거나 삭제할 수 있게 만들어야 합니다.

## 3. 알고리즘 및 엔진 포팅 가이드
- 현재 점수 산출 로직은 Python(engine.py, gyogwa.py, suneung.py)으로 작성되어 있습니다.
- 모바일 프레임워크(React Native, Flutter 등)로 포팅할 경우, JS/Dart 등에서 위 로직을 동일하게 구현해야 합니다. 
- 특히 **대학별 입시 요강 자동 적용 로직**(각 학기별 순수 데이터를 보존하여 대학별 맞춤 비율을 자동 계산하는 구조)을 완벽히 이식해야 합니다.

## 4. 대학 DB 연동
- 앱 구동에 필요한 대학별 데이터는 이 레포지토리의 universities.json을 사용합니다.
- 성적 데이터는 서버에 안 보내지만, 최신 DB 업데이트를 받아오기 위한 Read-Only 요청은 허용됩니다.
